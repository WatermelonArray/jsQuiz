"use strict";

let blackFrame = 0;
let timer = 0;
let reverse = false;

const fade = (canvas, context, switchAmim, to) => {

	const cw = canvas.width;
	const ch = canvas.height;

}

const swipe = (canvas, context) => {
	
	const cw = canvas.width;
	const ch = canvas.height;

}

const transitionSet = () => {}

export {transitionSet};